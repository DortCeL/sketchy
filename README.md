# sketchy?

Paste a job description. Gemini checks if it looks like a scam / shady posting.

## setup

```bash
npm install
cp .env.example .env.local
# put your GEMINI_API_KEY in .env.local
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
